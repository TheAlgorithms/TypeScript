import {isDivisible} from '../IsDivisible';

describe('Divisible', () => {
  
test('should return correct value', () => {
  
expect(isDivisible(1)).toBe("Number is Odd");
expect(isDivisible(2)).toBe("Number is Even");

});
});
